// Create two variables: top_speed and distance.
top_speed = 160;
distance = 300;

// Display the values referenced by the variables.
console.log('The top speed is');
console.log(top_speed);
console.log('The distance traveled is');
console.log(distance);

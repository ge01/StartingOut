// This program demonstrates how a floating-point
// number is displayed with no formatting.
var amountDue = 5000.0;
var monthlyPayment = amountDue / 12.0;
console.log('The monthly payment is '
             + monthlyPayment.toFixed(2));
